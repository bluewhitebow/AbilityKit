---
description: ET Demo SessionCoordinator 架构设计 - 基于 ET Framework 的战斗会话协调
alwaysApply: true
---

# ET Demo SessionCoordinator 架构设计

## 一、架构概述

ET Demo 采用 **SessionCoordinator 架构**，将 AbilityKit 的通用会话协调能力与 ET Framework 的组件系统有机结合。

### 1.1 核心定位

**关键理解**：
- **ET.Logic** = 逻辑层的**表现层**（数据缓存 + 入口协调）
- **ET.View** = 表现层的**渲染层**（读取缓存渲染）
- **moba.runtime** = 真正的**战斗逻辑世界层**（所有业务逻辑）

### 1.2 完整架构分层图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           完整架构分层图                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    ET.View (渲染层)                                    │   │
│  │                                                                       │   │
│  │   ETUnitViewComponent ─── 读取缓存并渲染（仅日志）                    │   │
│  │   ETUnitViewComponentSystem ─── 渲染更新逻辑                           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │ 读取缓存                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    ET.Logic (表现层数据缓存) ✅                       │   │
│  │                                                                       │   │
│  │   ETBattleEntityCacheComponent ─── 缓存 moba.core 快照数据           │   │
│  │   ETBattleSessionComponent ─── 持有 Coordinator 句柄                    │   │
│  │   ETInputComponent ─── 输入缓冲                                       │   │
│  │   ETUnitComponent ─── 单位管理                                        │   │
│  │   ETMobaBattleDriver ─── 入口协调器（通过 MobaEnterGameFlowService） │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │ 接收快照                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │              Coordinator 包 (com.abilitykit.coordinator)                │   │
│  │                                                                       │   │
│  │   SessionCoordinator ────────────── 会话协调器                          │   │
│  │   ├── ISyncAdapter ─────────────── 同步策略                          │   │
│  │   └── IBattleDriverHost ─────────── 驱动宿主                          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │              moba.runtime (战斗逻辑世界层) ⭐                         │   │
│  │              (com.abilitykit.demo.moba.runtime)                     │   │
│  │                                                                       │   │
│  │   MobaBattleDriverHost ────────────── 驱动宿主                        │   │
│  │   MobaEnterGameFlowService ───────── 入口服务（实体创建）              │   │
│  │   MobaLobbyInputSink ─────────────── 输入处理                         │   │
│  │   SkillExecutor ─────────────────── 技能执行                          │   │
│  │   DamagePipelineService ──────────── 伤害计算                          │   │
│  │   MobaSnapshotRouter ────────────── 快照聚合                          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │              Host 包 (com.abilitykit.host)                            │   │
│  │                                                                       │   │
│  │   HostRuntime + WorldManager ────────── 世界管理                       │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │              moba.core (ECS 世界层)                                 │   │
│  │              (com.abilitykit.demo.moba.runtime)                     │   │
│  │                                                                       │   │
│  │   ActorContext ───────────────────── 实体上下文                       │   │
│  │   MobaEntityManager ───────────────── 实体管理                        │   │
│  │   MobaActorRegistry ───────────────── ActorId 注册                    │   │
│  │   IWorldStateSnapshotProvider ──────── 快照提供者                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、核心设计原则

### 2.1 单一入口原则

**核心原则**：ET.Logic 层只通过以下接口与 moba.core 交互：

| 交互方向 | 入口接口 | 说明 |
|---------|---------|------|
| **输入提交** | `IWorldInputSink.Submit()` | 提交玩家输入（移动、技能等） |
| **实体创建** | `MobaEnterGameFlowService.ApplyGameStartSpec()` | 创建游戏实体 |
| **状态查询** | `IWorldStateSnapshotProvider` | 获取快照数据（通过回调） |

**禁止**：
- ❌ 直接访问 `MobaEntityManager` 或实体进行操作
- ❌ 直接调用 `ActorArchetypeFactory.Create()` 创建实体
- ❌ 直接调用 `MobaEntityManager.Register()` 注册实体
- ❌ 直接查询实体位置或属性

### 2.2 表现层职责分离

| 层 | 职责 | 禁止 |
|---|------|------|
| **ET.Logic** | - 缓存 moba.core 快照数据<br>- 管理输入缓冲<br>- 通过 `MobaEnterGameFlowService` 协调实体创建<br>- 驱动 SessionCoordinator | - 直接操作 moba.core 实体<br>- 渲染逻辑 |
| **ET.View** | - 读取缓存渲染<br>- 仅日志输出 | - 业务逻辑<br>- 修改缓存数据 |

---

## 三、核心组件

### 3.1 ETMobaBattleDriver（入口协调器）

**职责**：ET.Logic 层的入口协调器，通过 moba.core 服务与战斗逻辑世界交互。

```csharp
// ETMobaBattleDriver.cs - 入口协调器
public sealed class ETMobaBattleDriver : Entity, IBattleDriver
{
    // ✅ 正确：通过服务委托
    private void TriggerEnterGameSnapshot()
    {
        // 获取 MobaEnterGameFlowService
        if (!TryResolve<MobaEnterGameFlowService>(out var enterGameService))
            return;

        // 构建 MobaGameStartSpec
        var spec = new MobaGameStartSpec(in enterReq);

        // 委托给 moba.core 服务
        enterGameService.ApplyGameStartSpec(actorContext, in spec);

        // 从 MobaActorRegistry 获取创建的实体信息
        // 不直接操作实体，只查询
    }

    // ✅ 正确：通过 IWorldInputSink 提交输入
    public void SubmitMoveInput(int actorId, float x, float z)
    {
        _inputSink.Submit(frame, commands);
    }

    // ❌ 错误：直接创建实体
    // var built = ActorArchetypeFactory.Create(actorContext, info);  // ❌
    // entityManager.Register(actorId, entity, ...);  // ❌
}
```

### 3.2 ETBattleEntityCacheComponent

**职责**：缓存 moba.core 快照数据，供 ET.View 渲染使用。

```csharp
[ComponentOf(typeof(Scene))]
public class ETBattleEntityCacheComponent : Entity, IAwake, IDestroy
{
    private readonly Dictionary<int, EntityCacheData> _entityCache = new();
    public int CachedFrame { get; private set; }

    public void UpdateCache(int frame, IReadOnlyList<ActorTransformData> transforms);
    public bool TryGetPosition(int actorId, out float x, out float y, out float z);
}
```

### 3.3 MobaEnterGameFlowService（moba.core 入口服务）

**职责**：moba.core 内部的入口服务，负责创建实体和发布初始快照。

```csharp
// moba.runtime - MobaEnterGameFlowService.cs
public sealed class MobaEnterGameFlowService : IService
{
    public bool ApplyGameStartSpec(ActorContext actorContext, in MobaGameStartSpec spec)
    {
        // 内部调用 ActorSpawnPipeline 创建实体
        var built = ActorSpawnPipeline.BuildActorsFromEnterGameReqAndInitialize(
            actorContext, actorIds, registry, entities,
            effectiveReq,
            initializer: (entity, loadout) => { /* 初始化属性 */ },
            onActorBuilt: (entity, loadout) => { /* 发布 spawn 快照 */ });

        // 发布 EnterGameSnapshot 到视图层
        _snapshot.PublishEnterGameResPayload(...);
        _spawn.PublishSpawnPayload(...);

        return true;
    }
}
```

---

## 四、数据流

### 4.1 实体创建流程（单一入口）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          实体创建流程（单一入口）                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. ETMobaBattleDriver.OnAllPlayersReady()                                │
│     └─▶ TriggerEnterGameSnapshot()                                        │
│                                                                             │
│  2. 构建 MobaGameStartSpec                                               │
│     └─▶ 创建 EnterMobaGameReq                                            │
│     └─▶ 创建 MobaPlayerLoadout[]                                        │
│                                                                             │
│  3. 调用 MobaEnterGameFlowService.ApplyGameStartSpec()  ← 唯一入口     │
│     └─▶ ActorSpawnPipeline.BuildActorsFromEnterGameReqAndInitialize()    │
│     └─▶ 创建 ActorEntity（moba.core 内部）                               │
│     └─▶ 发布 ActorSpawnSnapshot                                          │
│                                                                             │
│  4. ETMobaBattleDriver 查询实体信息                                       │
│     └─▶ MobaActorRegistry.TryGet() → 获取 ActorId                       │
│     └─▶ MobaPlayerActorMapService → 获取 PlayerId -> ActorId 映射        │
│                                                                             │
│  5. 创建 ETUnit（ET 表现层单位）                                          │
│     └─▶ unitComponent.CreateUnit(actorId, ...)                           │
│                                                                             │
│  6. 发送 EnterGameSnapshot 到视图层                                        │
│     └─▶ ViewSink.OnEnterGameSnapshot()                                   │
│                                                                             │
│  ❌ 禁止：ET.Logic 直接调用 ActorArchetypeFactory.Create()              │
│  ❌ 禁止：ET.Logic 直接调用 MobaEntityManager.Register()               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 快照同步流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          快照同步流程                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. AdvanceFrame (MobaBattleDriverHost)                                    │
│     └─▶ _hostRuntime.Tick(deltaTime)                                     │
│     └─▶ _coordinator.Tick(deltaTime)                                     │
│                                                                             │
│  2. TryGetTransformSnapshot (MobaBattleDriverHost)                        │
│     └─▶ _world.Services.TryResolve<IWorldStateSnapshotProvider>()        │
│     └─▶ provider.TryGetSnapshot(frame, out snapshot)                      │
│                                                                             │
│  3. SetTransformSnapshotCallback (回调)                                   │
│     └─▶ OnTransformSnapshotReceived(frame, transforms)                    │
│                                                                             │
│  4. ETBattleDriverBridge.OnTransformSnapshotReceived()                    │
│     ├─▶ cache.UpdateCache(frame, actorTransforms)                       │
│     └─▶ EventSystem.Instance.Publish<ActorMoveEvent>()                  │
│                                                                             │
│  5. ET.View 订阅事件                                                      │
│     └─▶ ActorMoveEventHandler 接收事件                                  │
│     └─▶ 渲染实体                                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.3 输入处理流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           输入处理流程                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. AutoTest (ETBattleAutoTestComponent)                                  │
│     └─▶ AddMoveCommand(frame, actorId, x, y)                              │
│                                                                             │
│  2. InputBuffer (ETInputComponent)                                        │
│     └─▶ 存储命令到缓冲                                                    │
│                                                                             │
│  3. ProcessInput (ETInputComponentSystem)                                 │
│     └─▶ ForwardMoveToCoordinator()                                        │
│     └─▶ session.SubmitLocalInput(input)                                  │
│                                                                             │
│  4. SessionCoordinator (LocalSyncAdapter)                                 │
│     └─▶ _pendingInputs.Add(input)                                        │
│                                                                             │
│  5. ProcessLogicFrame (LocalSyncAdapter)                                 │
│     └─▶ driverHost.SubmitInputs(inputs)                                  │
│                                                                             │
│  6. MobaBattleDriverHost.SubmitInputs()                                   │
│     └─▶ _inputSink.Submit(frame, commands)  ← 唯一入口                  │
│                                                                             │
│  7. moba.core (IWorldInputSink 实现)                                     │
│     └─▶ MobaLobbyInputSink.HandleMove()                                  │
│     └─▶ entity.AddMoveInput(x, z)  ← moba.core 内部操作               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 五、ET.View 层职责

### 5.1 View 层定位

**ET.View 是渲染层，只读取 ET.Logic 的缓存数据来渲染。**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          ET.View 职责                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ✅ 正确：View 层职责                                                     │
│  ├── 读取 ETBattleEntityCacheComponent 缓存                               │
│  ├── 渲染实体（GameObject/ASCII/日志）                                    │
│  └── 订阅缓存变化事件（可选）                                              │
│                                                                             │
│  ❌ 禁止：View 层禁止职责                                                  │
│  ├── 直接访问 moba.core 实体                                              │
│  ├── 修改 ETBattleEntityCacheComponent 数据                               │
│  ├── 业务逻辑处理（伤害计算、Buff 等）                                    │
│  └── 创建 moba.core 实体                                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 View 层事件订阅

ET.View 通过 ET 框架的事件系统订阅 ET.Logic 发布的事件：

```csharp
// ET.View 的 Handler 示例
[Event(SceneType.DemoBattle)]
public class ActorMoveEventHandler : AEvent<Scene, ActorMoveEvent>
{
    protected override async ETTask Run(Scene scene, ActorMoveEvent args)
    {
        Log.Info($"[View] Actor moved: MobaActorId={args.MobaActorId} at ({args.X}, {args.Y})");
    }
}
```

---

## 六、禁止事项

### 6.1 单一入口禁止

| 禁止 | 说明 | 正确做法 |
|-----|------|---------|
| ❌ 直接访问 `MobaEntityManager` | 违反单一入口原则 | 通过服务接口查询 |
| ❌ 直接调用 `ActorArchetypeFactory.Create()` | 直接操作 moba.core | 通过 `MobaEnterGameFlowService` |
| ❌ 直接调用 `MobaEntityManager.Register()` | 直接操作 moba.core | 通过 `MobaEnterGameFlowService` |
| ❌ 直接查询实体位置 | 违反单一入口原则 | 通过快照回调更新缓存 |

### 6.2 层级禁止

| 禁止 | 说明 | 正确做法 |
|-----|------|---------|
| ❌ ET.View 访问 moba.core | 跨层访问 | 通过 ET.Logic 缓存间接访问 |
| ❌ ET.Logic 包含渲染逻辑 | 职责混乱 | ET.View 负责渲染 |
| ❌ 循环依赖 | 架构破坏 | 单向依赖 |

---

## 七、关键文件

### 7.1 ET.Logic 层

| 文件 | 职责 |
|-----|------|
| `Model/Driver/ETMobaBattleDriver.cs` | 入口协调器（通过服务与 moba.core 交互） |
| `Model/Battle/ETBattleEntityCacheComponent.cs` | 实体状态缓存 |
| `Model/Battle/ETBattleSessionComponent.cs` | 会话组件 |
| `Hotfix/Battle/ETBattleDriverBridge.cs` | 驱动桥接（快照回调） |
| `Hotfix/Battle/ETInputComponentSystem.cs` | 输入处理 |

### 7.2 moba.runtime 层

| 文件 | 职责 |
|-----|------|
| `Services/EnterGame/MobaEnterGameFlowService.cs` | 实体创建入口服务 |
| `Services/Input/MobaLobbyInputSink.cs` | 输入处理（IWorldInputSink 实现） |
| `Services/Snapshot/MobaSnapshotRouter.cs` | 快照聚合 |
| `Services/Skill/Cast/SkillExecutor.cs` | 技能执行 |
| `Services/Combat/Damage/DamagePipelineService.cs` | 伤害计算 |
| `Services/EntityManager/MobaEntityManager.cs` | 实体管理 |
| `Util/Generator/ActorSpawnPipeline.cs` | 实体生成管线 |

### 7.3 ET.View 层

| 文件 | 职责 |
|-----|------|
| `Hotfix/Battle/ActorSpawnEventHandler.cs` | 单位创建事件处理 |
| `Hotfix/Battle/ActorMoveEventHandler.cs` | 单位移动事件处理 |
| `Model/Unit/ViewComponents/ETUnitViewComponent.cs` | 单位视图组件 |

---

## 八、架构检查清单

开发时检查以下问题：

```
□ 实体创建是否通过 MobaEnterGameFlowService.ApplyGameStartSpec()？
□ 输入是否通过 IWorldInputSink.Submit() 提交？
□ 快照是否通过回调更新到 ETBattleEntityCacheComponent？
□ ET.View 是否只读取缓存，不访问 moba.core？
□ 是否有代码直接调用 ActorArchetypeFactory.Create()？
□ 是否有代码直接调用 MobaEntityManager.Register()？
```
