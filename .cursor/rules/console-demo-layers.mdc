# Console Demo 层级架构规范

## 概述

本文档定义 Console Demo 项目中 **逻辑层** 和 **表现层** 的职责边界。

---

## 一、核心原则

```
┌─────────────────────────────────────────────────────────────────────┐
│                         层级架构原则                                  │
│                                                                     │
│   1️⃣ 逻辑层负责计算，表现层负责渲染                                  │
│      - 逻辑层生成最终状态                                            │
│      - 表现层只渲染结果                                              │
│                                                                     │
│   2️⃣ 事件携带完整状态                                               │
│      - 事件应包含渲染所需的所有数据                                  │
│      - 表现层不做任何计算                                            │
│                                                                     │
│   3️⃣ 单向数据流                                                     │
│      逻辑层 ──▶ 事件 ──▶ 表现层                                     │
│                     │                                                │
│                     ▼                                                │
│               只做渲染，不回传                                        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 二、逻辑层职责清单

逻辑层负责所有 **游戏规则** 和 **数值计算**。

| 职责 | 说明 | 示例 |
|-----|------|-----|
| **伤害计算** | 计算实际伤害值 | 基础伤害、护甲减免、暴击 |
| **HP/MaxHp 管理** | 管理生命值状态 | 扣血、加血、上限限制 |
| **死亡判定** | 判断实体是否死亡 | HP ≤ 0 时触发死亡 |
| **Buff 管理** | 添加/移除 Buff | Buff 叠加、持续时间 |
| **冷却管理** | 技能冷却同步 | 全局冷却、槽位冷却 |
| **技能流程执行** | 技能施法流程 | 吟唱、弹道、命中 |
| **目标搜索** | 搜索有效目标 | 范围搜索、优先规则 |
| **弹道管理** | 子弹/投射物逻辑 | 移动、碰撞检测 |
| **区域效果** | AOE 区域逻辑 | 火圈、结界 |

---

## 三、表现层职责清单

表现层负责所有 **用户可见的反馈** 和 **输入采集**。

| 职责 | 说明 | 示例 |
|-----|------|-----|
| **渲染实体** | 显示/隐藏 GameObject | 角色模型、特效 |
| **播放特效** | 视觉/声音反馈 | 技能光效、命中特效 |
| **显示飘字** | 数字/文字浮动显示 | 伤害数字、加血数字 |
| **输入采集** | 采集玩家操作 | 键盘、鼠标、触摸 |
| **显示 HUD** | 用户界面元素 | 血条、技能图标、地图 |
| **位置插值** | 平滑显示移动 | 网络延迟补偿 |
| **状态同步** | 同步逻辑层状态 | 从快照更新显示 |

---

## 四、禁止事项

### 4.1 表现层禁止行为

| 禁止 | 原因 | 正确做法 |
|-----|------|---------|
| ❌ 修改 HP 值 | 违反单向数据流原则 | 事件应携带最终 HP |
| ❌ 决定死亡 | 死亡判定属于逻辑层 | 事件应携带 `IsDead` 标志 |
| ❌ 管理冷却 | 冷却状态由逻辑层控制 | 表现层只显示冷却状态 |
| ❌ 计算伤害 | 伤害计算属于逻辑层 | 事件应携带伤害值 |
| ❌ 搜索目标 | 目标搜索规则属于逻辑层 | 逻辑层提供目标结果 |

### 4.2 禁止直接访问逻辑层服务

**核心原则**：表现层与逻辑层必须完全解耦。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         解耦架构原则                                         │
│                                                                             │
│   表现层 ──▶ 输入服务 ──▶ 逻辑层                                          │
│                    ▲                                                        │
│                    │                                                        │
│              只通过单一                          逻辑层可运行在：                                   │
│              输入接口交互                       - 本地（Console Demo）                             │
│                                              - 服务器（完整游戏）                                 │
│                                                                             │
│   ❌ 禁止：表现层直接获取逻辑层服务                                        │
│   ✅ 正确：通过事件/输入接口与逻辑层通信                                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

| 禁止 | 原因 | 正确做法 |
|-----|------|---------|
| ❌ 直接获取 `IWorldResolver` | 违反解耦原则 | 通过事件接口通信 |
| ❌ 直接获取 `MobaActorRegistry` | 耦合逻辑层内部状态 | 通过事件获取数据 |
| ❌ 直接调用逻辑层服务 | 无法切换到服务器运行 | 通过统一输入接口 |
| ❌ 直接读取逻辑层属性 | 耦合具体实现 | 通过事件携带状态 |

### 4.3 错误示例

```csharp
// ❌ 错误：表现层直接获取逻辑层服务
public sealed class MobaCoreSkillExecutor
{
    private readonly IWorldResolver _worldServices;      // ❌ 持有逻辑层引用
    private readonly MobaActorRegistry _actorRegistry;   // ❌ 直接访问逻辑层状态

    private void ExecuteFallback(...)
    {
        // ❌ 直接从逻辑层获取数据
        if (_actorRegistry.TryGet(targetActorId, out var targetEntity))
        {
            var attrs = targetEntity.GetMobaAttrs();
            float currentHp = attrs.Hp;
            // ...
        }
    }
}

// ✅ 正确：通过事件传递逻辑层状态
public readonly struct DamageEvent
{
    public int TargetId { get; }
    public int SourceId { get; }
    public float Damage { get; }
    public float CurrentHp { get; }        // 逻辑层计算后通过事件传递
    public float MaxHp { get; }
    public bool IsDead { get; }
}
```

### 4.4 正确示例：表现层只做渲染

```csharp
// ✅ 正确：表现层只接收事件并渲染
public sealed class ConsoleViewEventSink
{
    private readonly IConsoleBattleView _battleView;

    private void OnDamage(DamageEvent evt)
    {
        // ✅ 只做渲染，不做任何计算或服务调用
        _battleView.ShowFloatingText(evt.TargetId, $"-{evt.Damage:F0}", false);
        _battleView.UpdateEntityHp(evt.TargetId, evt.CurrentHp, evt.MaxHp);

        if (evt.IsDead)
        {
            _battleView.ShowFloatingText(evt.TargetId, "DIED!", false);
        }
    }
}
```

### 4.5 正确示例：逻辑层通过回调输出，表现层发布事件

```csharp
// ✅ 正确：逻辑层只通过回调输出，不直接发布 BattleEventBus
public sealed class MobaCoreSkillExecutor
{
    // 事件回调
    public event Action<int, int, float, int, float, float, bool> OnDamageDealt;
    public event Action<int> OnActorDied;

    public void OnSkillInput(SkillInputEvent input)
    {
        // 逻辑层计算伤害
        float damage = CalculateDamage(input);
        float newHp = Math.Max(0, target.Hp - damage);
        bool isDead = newHp <= 0;

        // 通过回调输出，不直接发布 BattleEventBus
        OnDamageDealt?.Invoke(
            input.CasterActorId,
            input.TargetActorId,
            damage,
            input.SkillId,
            newHp,
            target.MaxHp,
            isDead);

        if (isDead)
        {
            OnActorDied?.Invoke(input.TargetActorId);
        }
    }
}

// 表现层（Bootstrapper）负责将回调发布到 BattleEventBus
mobaSkillExecutor.OnDamageDealt += (sourceId, targetId, damage, skillId, currentHp, maxHp, isDead) =>
{
    BattleEventBus.Publish(new DamageEvent
    {
        SourceId = sourceId,
        TargetId = targetId,
        Damage = damage,
        CurrentHp = currentHp,
        MaxHp = maxHp,
        IsDead = isDead
    });
};
```

### 4.2 正确示例

```csharp
// ❌ 错误：表现层计算最终 HP
private void OnDamage(DamageEvent evt)
{
    var entity = GetEntity(evt.TargetId);
    var finalHp = entity.Hp - evt.Damage;  // ❌ 越权计算
    UpdateHpDisplay(finalHp, entity.MaxHp);
}

// ✅ 正确：事件携带完整状态
private void OnDamage(DamageEvent evt)
{
    _battleView.ShowFloatingText(evt.TargetId, $"-{evt.Damage:F0}", false);
    _battleView.UpdateEntityHp(evt.TargetId, evt.CurrentHp, evt.MaxHp);  // ✅ 直接渲染
}
```

### 4.3 死亡处理正确示例

```csharp
// ❌ 错误：表现层决定移除实体
private void OnEntityDestroyed(EntityDestroyedEvent evt)
{
    _battleView.ShowFloatingText(evt.ActorId, "DIED!", false);
    _entityDisplay.Remove(evt.ActorId);  // ❌ 表现层决定移除
}

// ✅ 正确：响应逻辑层的死亡决定
private void OnEntityDestroyed(EntityDestroyedEvent evt)
{
    if (!evt.IsDead) return;  // 逻辑层已判定死亡
    _battleView.ShowFloatingText(evt.ActorId, "DIED!", false);
    // 移除由逻辑层通过 EntityDespawnSnapshot 触发
}
```

---

## 五、事件设计规范

### 5.0 BattleEventBus 使用原则

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         BattleEventBus 使用规范                              │
│                                                                             │
│   BattleEventBus 是表现层的内部事件总线                                       │
│   所有发布者和订阅者都在表现层内部                                           │
│                                                                             │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │ 表现层（Console Demo / moba.view）                                │   │
│   │                                                                   │   │
│   │   输入采集 ──▶ BattleEventBus ──▶ ViewEventSink ──▶ 渲染      │   │
│   │                                                                   │   │
│   │   逻辑层执行 ──▶ BattleEventBus ──▶ ViewEventSink ──▶ 渲染    │   │
│   │                                                                   │   │
│   │   ✅ 所有组件都可以发布/订阅                                        │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

| 组件 | 职责 | BattleEventBus |
|-----|------|---------------|
| `ConsoleInputFeature` | 输入采集 | ✅ 发布输入事件 |
| `ConsoleSyncFeature` | 帧同步 | ✅ 发布帧同步事件 |
| `MobaCoreSkillExecutor` | 逻辑层技能执行 | ✅ 发布伤害/死亡事件 |
| `ConsoleViewEventSink` | 视图渲染 | ✅ 订阅事件 |

### 5.1 正确示例

```csharp
// ✅ 正确：逻辑层直接发布 BattleEventBus
public sealed class MobaCoreSkillExecutor
{
    public bool ExecuteSkill(int actorId, int slot, int targetId)
    {
        var damage = CalculateDamage(...);

        // 直接发布事件
        var evt = new DamageEvent { ... };
        BattleEventBus.Publish(in evt);

        return true;
    }
}

// ✅ 正确：表现层订阅 BattleEventBus
public sealed class ConsoleViewEventSink
{
    public void Initialize()
    {
        BattleEventBus.Subscribe<DamageEvent>(OnDamage);
    }

    private void OnDamage(DamageEvent evt)
    {
        // 只做渲染
        _battleView.ShowFloatingText(evt.TargetId, $"-{evt.Damage:F0}", false);
    }
}
```

```csharp
public readonly struct DamageEvent
{
    public int TargetId { get; }
    public int SourceId { get; }
    public float Damage { get; }          // 伤害值（用于显示）
    public float CurrentHp { get; }        // 最终 HP（渲染用）
    public float MaxHp { get; }            // 最大 HP（渲染用）
    public bool IsDead { get; }            // 是否死亡（判定结果）
    public bool IsCritical { get; }        // 是否暴击（用于特效）
}
```

### 5.2 HealEvent 正确设计

```csharp
public readonly struct HealEvent
{
    public int TargetId { get; }
    public int SourceId { get; }
    public float Amount { get; }           // 治疗量（用于显示）
    public float CurrentHp { get; }        // 最终 HP（渲染用）
    public float MaxHp { get; }            // 最大 HP（渲染用）
}
```

---

## 六、架构组件说明

### 6.1 组件职责

| 组件 | 位置 | 职责 |
|-----|------|------|
| `ConsoleViewEventSink` | `View/` | 订阅 BattleEventBus，渲染视图 |
| `MobaCoreSkillExecutor` | `MobaCore/` | 逻辑层技能执行，发布 BattleEventBus |
| `ConsoleSkillInputHandler` | `MobaCore/` | 封装 MobaCoreSkillExecutor 调用 |
| `ConsoleInputFeature` | `Core/Input/` | 输入采集，发布输入事件 |
| `ConsoleSyncFeature` | `Battle/Sync/` | 帧同步，发布帧同步事件 |

---

## 七、完整数据流

### 7.1 技能施放流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         技能施放完整数据流                                    │
│                                                                             │
│  【输入层】                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ ConsoleInputFeature                                                   │   │
│  │   - 采集玩家输入（HudSkillClickSlot）                               │   │
│  │   - 发布 SkillExecutedEvent                                          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                   │                                        │
│                                   ▼                                        │
│  【逻辑层】                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ MobaCoreSkillExecutor                                                 │   │
│  │   - 计算伤害（FallbackDamageCalculator）                             │   │
│  │   - 发布 DamageEvent, EntityDestroyedEvent                          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                   │                                        │
│                                   ▼                                        │
│  【表现层】                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ ConsoleViewEventSink                                                   │   │
│  │   - 订阅 BattleEventBus                                              │   │
│  │   - 显示伤害飘字、更新 HP                                            │   │
│  │   - 只做渲染，不做计算                                               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 与 moba.view 对齐

```
Console Demo                           moba.view
─────────────────────────             ─────────────────────────
MobaCoreSkillExecutor           <───   (moba.core 内部)

BattleEventBus                   ──▶   BattleEventBus
        │                                │
        ▼                                ▼
ConsoleViewEventSink            <───   BattleViewEventSink
```

---

## 八、Console Demo 特殊说明

Console Demo 是一个 **简化实现**，用于快速验证和调试。

### 8.1 已修复的架构问题

| 文件 | 问题 | 修复方式 |
|-----|------|---------|
| `MobaCoreSkillExecutor` | 直接获取 `IWorldResolver` 和 `MobaActorRegistry` | ✅ 已重构为纯计算类，不持有逻辑层引用 |
| `BattleEventBus` 越权调用 | 逻辑层直接发布表现层事件 | ✅ 改为回调，由桥接层发布 |

### 8.2 当前存在的简化实现

| 文件 | 问题 | 严重程度 | 处理方式 |
|-----|------|---------|---------|
| `ConsoleSkillExecutor` | 冷却本地模拟 | 低 | 保留（简化实现） |

### 8.3 组件关系图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         Console Demo 组件关系                                 │
│                                                                             │
│  【输入】                                                                   │
│  ┌─────────────────┐                                                        │
│  │ ConsoleInputFeature │                                                    │
│  └────────┬────────┘                                                        │
│           │ 调用 ISkillInputHandler                                          │
│           ▼                                                                 │
│  ┌─────────────────┐                                                        │
│  │ ISkillInputHandler │                                                    │
│  └────────┬────────┘                                                        │
│           │ 实现                                                            │
│           ▼                                                                 │
│  ┌─────────────────────────┐                                                │
│  │ ConsoleSkillInputHandler │                                             │
│  └────────┬────────────────┘                                                │
│           │ 调用                                                            │
│           ▼                                                                 │
│  ┌─────────────────────────┐                                                │
│  │ MobaCoreSkillExecutor   │ ◀── 逻辑层（不持有外部引用）                  │
│  │   - 伤害计算            │                                                │
│  │   - 回调输出            │                                                │
│  └────────┬────────────────┘                                                │
│           │ 回调                                                           │
│           ▼                                                                 │
│  ┌─────────────────────────┐                                                │
│  │ BattleTriggerEventViewBridge │ ◀── 桥接层                               │
│  └────────┬────────────────┘                                                │
│           │ 调用接口                                                         │
│           ▼                                                                 │
│  ┌─────────────────────────┐                                                │
│  │ IBattleViewEventSink    │                                                │
│  └────────┬────────────────┘                                                │
│           │ 实现                                                            │
│           ▼                                                                 │
│  ┌─────────────────────────┐                                                │
│  │ ConsoleViewEventSink    │ ◀── 表现层（只做渲染）                        │
│  │   - 显示飘字            │                                                │
│  │   - 更新 HP             │                                                │
│  └─────────────────────────┘                                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 8.4 简化实现说明

Console Demo 中存在一些简化实现：

| 组件 | 简化内容 | 说明 |
|-----|---------|------|
| `FallbackDamageCalculator` | 使用固定伤害值 | 完整实现应从配置读取 |
| `ConsoleSkillExecutor` | 本地冷却模拟 | 完整实现应由逻辑层管理 |

### 6.3 架构演进路径

```
当前简化实现：
┌─────────────────────────────────────────────────────────────────────┐
│  Console Demo                                                      │
│  ┌─────────────────┐    ┌─────────────────────┐                   │
│  │ 表现层（View）    │    │  技能执行（MobaCore）  │                   │
│  │                 │    │                     │                   │
│  │ ConsoleSkill    │    │ MobaCoreSkill       │                   │
│  │ Executor        │───▶│ Executor            │───▶ 事件          │
│  │ (简化冷却)       │    │ (简化伤害计算)        │                   │
│  └─────────────────┘    └─────────────────────┘                   │
└─────────────────────────────────────────────────────────────────────┘

目标架构（逻辑层可分离）：
┌─────────────────────────────────────────────────────────────────────┐
│  表现层（View）        │           │  逻辑层（可运行在服务器）          │
│  ┌─────────────────┐  │  事件流   │  ┌─────────────────────┐       │
│  │ ConsoleView     │◀─│───────────│─▶│ 技能系统              │       │
│  │ EventSink       │  │           │  │ 伤害计算              │       │
│  └─────────────────┘  │           │  │ Buff 管理            │       │
│                       │           │  └─────────────────────┘       │
│  ┌─────────────────┐  │  输入流   │  ┌─────────────────────┐       │
│  │ 输入采集         │──│───────────│─▶│ 技能执行器            │       │
│  │ (统一接口)       │  │           │  │ (通过接口通信)        │       │
│  └─────────────────┘  │           │  └─────────────────────┘       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 七、架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                      MOBA Demo 架构层次                              │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐  │
│   │                    Console Demo                              │  │
│   │                                                             │  │
│   │   ┌─────────────────────────────────────────────────────┐  │  │
│   │   │                 表现层 (View Layer)                    │  │  │
│   │   │                                                     │  │  │
│   │   │   ConsoleViewEventSink     - 事件响应                │  │  │
│   │   │   ConsoleBattleView        - 渲染输出                 │  │  │
│   │   │   ConsoleEntityDisplay     - 实体显示                │  │  │
│   │   │   ConsoleEffectExecution   - 特效表现                │  │  │
│   │   │                                                     │  │  │
│   │   │   职责：接收事件，只做渲染，无计算                    │  │  │
│   │   └─────────────────────────────────────────────────────┘  │  │
│   │                           ▲                                 │  │
│   │                           │ 事件（携带完整状态）            │  │
│   │                           │                                │  │
│   │   ┌─────────────────────────────────────────────────────┐  │  │
│   │   │                 逻辑层 (Logic Layer)                 │  │  │
│   │   │                                                     │  │  │
│   │   │   MobaCoreSkillExecutor    - 技能执行               │  │  │
│   │   │   MobaBuffService          - Buff 管理               │  │  │
│   │   │   MobaProjectileService    - 弹道管理                │  │  │
│   │   │   MobaDamageCalculator     - 伤害计算                │  │  │
│   │   │                                                     │  │  │
│   │   │   职责：所有计算和判定                               │  │  │
│   │   └─────────────────────────────────────────────────────┘  │  │
│   │                                                             │  │
│   └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 八、快速检查清单

在提交代码前，检查以下问题：

- [ ] 表现层代码是否包含任何数值计算？（伤害、HP、Buff 层数等）
- [ ] 事件是否携带渲染所需的完整数据？
- [ ] 表现层是否直接修改了逻辑层状态？
- [ ] 死亡判定是否由表现层决定？
- [ ] 表现层是否直接获取逻辑层服务？（如 `IWorldResolver`、`MobaActorRegistry`）
- [ ] 是否有跨层直接调用逻辑层内部 API 的代码？

如果以上任何问题回答"是"，则需要重构。
