---
description: AbilityKit src 与 Unity/Packages 源码关系 - 源码位置、引用方式、快速定位
alwaysApply: true
---

# AbilityKit src 与 Unity/Packages 源码关系

## 核心关系

```
AbilityKit/
├── src/                           # .NET SDK 项目（编译/测试用）
│   ├── AbilityKit.Core/
│   │   └── AbilityKit.Core.csproj ──────→ 引用 ../Unity/Packages/com.abilitykit.core/
│   ├── AbilityKit.Triggering/
│   │   └── AbilityKit.Triggering.csproj
│   └── ...
│
└── Unity/Packages/                # Unity Package 源码（实际运行用）
    ├── com.abilitykit.core/
    │   └── Runtime/**/*.cs
    ├── com.abilitykit.triggering/
    │   └── Runtime/**/*.cs
    └── ...
```

## 源码共享模式

| 目录 | 用途 | 编译系统 |
|------|------|----------|
| `Unity/Packages/` | **唯一源码位置** | Unity asmdef |
| `src/` | .NET 项目引用源码 | .NET SDK (dotnet build) |

**重要**：Unity/Packages 目录是唯一的源码位置，src 目录的 .csproj 通过 `<Compile Include>` 引用它们。

## src .csproj 引用方式

```xml
<!-- src/AbilityKit.Core/AbilityKit.Core.csproj -->
<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <!-- 引用 Unity/Packages 源码，排除 Unity 特定文件 -->
    <Compile Include="../../Unity/Packages/com.abilitykit.core/Runtime/**/*.cs" 
             Exclude="../../Unity/Packages/com.abilitykit.core/Runtime/**/UnityJsonSettingsBootstrap.cs" />
  </ItemGroup>
</Project>
```

## 查找代码的位置

| 场景 | 查找位置 | 说明 |
|------|----------|------|
| **阅读/修改代码** | `Unity/Packages/` | 唯一源码位置 |
| **运行 dotnet build** | `src/` | 使用 .csproj 编译 |
| **Unity 编辑器** | `Unity/Packages/` | Unity 自动编译 |
| **编写新代码** | `Unity/Packages/` | 在此处创建/修改 |

## 包名对应关系

| Unity Package | src 项目 |
|---------------|---------|
| `com.abilitykit.core` | `AbilityKit.Core` |
| `com.abilitykit.triggering` | `AbilityKit.Triggering` |
| `com.abilitykit.flow` | `AbilityKit.Flow` |
| `com.abilitykit.gameplaytags` | `AbilityKit.GameplayTags` |
| `com.abilitykit.modifiers` | `AbilityKit.Modifiers` |
| `com.abilitykit.pipeline` | `AbilityKit.Pipeline` |
| `com.abilitykit.hfsm` | `AbilityKit.HFSM.Core` |
| `com.abilitykit.behavior` | `AbilityKit.Behavior` |

## 命名空间与目录映射

```
Unity/Packages/com.abilitykit.core/Runtime/Core/Common/Marker/
├── MarkerAttribute.cs
├── MarkerRegistry.cs
└── MarkerScanner.cs

对应命名空间：AbilityKit.Core.Common.Marker
```

## 工作流程

```
1. 修改代码 → 在 Unity/Packages/ 目录下修改 .cs 文件
2. .NET 开发/测试 → cd src && dotnet build
3. Unity 开发/运行 → Unity 编辑器自动编译
```

## 禁止事项

❌ **禁止**：在 src 目录下创建与 Unity/Packages 重复的源码文件
❌ **禁止**：修改 src 目录下的源码（它们只是引用）
❌ **禁止**：在 src 目录添加 .asmdef 文件

## 创建新包的步骤

1. 在 `Unity/Packages/` 创建包结构：
   ```
   Unity/Packages/com.abilitykit.newfeature/
   ├── Runtime/
   │   └── com.abilitykit.newfeature.asmdef
   └── package.json
   ```

2. 在 `src/` 创建 .NET 项目：
   ```
   src/AbilityKit.NewFeature/
   └── AbilityKit.NewFeature.csproj  ← 引用 Unity/Packages 源码
   ```

3. 如需 Samples，在 `src/AbilityKit.Samples/` 添加示例代码

## 快速定位指南

| 想要找... | 去这里 |
|-----------|--------|
| MarkerAttribute 基类 | `Unity/Packages/com.abilitykit.core/Runtime/Core/Common/Marker/` |
| Triggering 系统 | `Unity/Packages/com.abilitykit.triggering/Runtime/` |
| GameplayTags | `Unity/Packages/com.abilitykit.gameplaytags/Runtime/` |
| 框架核心接口 | `Unity/Packages/com.abilitykit.core/Runtime/` |
| 示例代码 | `src/AbilityKit.Samples/Samples/` |
| 配置文件 | `src/AbilityKit.Samples/Configs/` |
